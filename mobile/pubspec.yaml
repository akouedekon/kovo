name: kovo_mobile
description: Kovo ridesharing mobile app
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.12.2
  provider: ^5.0.0
  shared_preferences: ^2.0.0
  flutter_map: ^1.0.0
  latlong: ^0.6.0
  url_launcher: ^6.0.0
  webview_flutter: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
