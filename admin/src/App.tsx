import React from 'react';

export default function App(){
  return (
    <div style={{padding:20}}>
      <h1>Kovo — Admin</h1>
      <p>Tableau de bord administratif (utilisateurs, trajets, réservations)</p>
    </div>
  )
}
